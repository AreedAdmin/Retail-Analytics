# Pinned to the VALIDATED environment (everything — notebook, dashboard,
# Ollama client — was smoke-tested on these exact versions). The previous
# pins included versions that do not exist on PyPI (pandas 3.0.3, numpy
# 2.4.4, scikit-learn 1.8.0) and would fail `pip install` on HF Spaces.
# Deployment path = Ollama Cloud for the AI layer (see deploy.md), so the
# heavy local-LLM stack (torch/transformers) is OPTIONAL and commented out
# to keep the free CPU Space build small and reliable.

# Application and UI
gradio==6.9.0
plotly==5.24.1
pydantic==2.12.3
python-dotenv==1.0.0

# Data Processing
pandas==2.3.3
numpy==2.0.2
pyarrow==19.0.1
scipy==1.15.1

# Machine Learning and Forecasting
scikit-learn==1.6.1          # MUST match the version the joblib artifact is trained with
statsmodels==0.14.6
# xgboost  # Optional, if memory budget allows

# LLM and AI Integration
ollama==0.4.7                # used for Ollama Cloud (gpt-oss:120b-cloud) + local
# Optional API fallbacks (only needed if you set the matching API key):
# openai==1.28.1
# anthropic==0.28.0
# tiktoken==0.7.0

# Optional in-Space local open-model backend (NOT used on the Ollama Cloud
# path; uncomment only for a ZeroGPU/CPU local-model deployment — torch is
# large and the most common free-Space build failure):
# transformers==4.44.2
# torch==2.4.0
# accelerate==0.33.0
# spaces==0.30.2

# Quality and Tooling (dev only)
pytest==7.4.4
ruff==0.3.7
black==24.3.0

# Additional utilities
requests==2.32.5
python-dateutil==2.8.2
